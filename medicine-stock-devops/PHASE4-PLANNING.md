# Phase 4: Kubernetes Deployment Planning\n\n**Points**: 25 pts\n\n---\n\n## Overview\n\nPhase 4 focuses on deploying the Medicine Stock application to the Kubernetes cluster, leveraging the infrastructure provisioned in Phase 3 (Terraform + Ansible).\n\n---\n\n## Deliverables\n\n### 1. Update Kubernetes Deployment Manifest (12 pts)\n\n**File**: `k8s/deployment.yaml`\n\n**Required Changes**:\n\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: medicine-stock\n  namespace: medicine-stock  # From Phase 3\n  labels:\n    app: medicine-stock\n\nspec:\n  replicas: 2  # ← Scale requirement\n  \n  selector:\n    matchLabels:\n      app: medicine-stock\n  \n  template:\n    metadata:\n      labels:\n        app: medicine-stock\n    \n    spec:\n      serviceAccountName: medicine-stock-sa  # From Phase 3\n      \n      containers:\n      - name: medicine-stock\n        image: {{DOCKER_IMAGE}}:{{IMAGE_TAG}}  # ← Must update\n        imagePullPolicy: IfNotPresent\n        \n        ports:\n        - containerPort: 5000\n          name: http\n        \n        # Environment from ConfigMap + Secret (Phase 3)\n        envFrom:\n        - configMapRef:\n            name: medicine-stock-config\n        - secretRef:\n            name: medicine-stock-secrets\n        \n        # Readiness Probe (detect when ready)\n        readinessProbe:\n          httpGet:\n            path: /\n            port: 5000\n          initialDelaySeconds: 10\n          periodSeconds: 10\n          timeoutSeconds: 5\n          failureThreshold: 3\n        \n        # Liveness Probe (detect if stuck)\n        livenessProbe:\n          httpGet:\n            path: /\n            port: 5000\n          initialDelaySeconds: 30\n          periodSeconds: 30\n          timeoutSeconds: 5\n          failureThreshold: 3\n        \n        # Resource Limits\n        resources:\n          requests:\n            cpu: 100m\n            memory: 128Mi\n          limits:\n            cpu: 500m\n            memory: 512Mi\n        \n        # Volume Mounts (if persistent DB needed)\n        # volumeMounts:\n        # - name: db-volume\n        #   mountPath: /app\n      \n      # Volumes (optional for persistent state)\n      # volumes:\n      # - name: db-volume\n      #   persistentVolumeClaim:\n      #     claimName: medicine-stock-db\n\n  # Deployment Strategy\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxSurge: 1\n      maxUnavailable: 0\n```\n\n**Key Features**:\n- ✅ Replicas: 2 (high availability)\n- ✅ ServiceAccount: Uses Phase 3 RBAC\n- ✅ Environment: From ConfigMap + Secret\n- ✅ Image: Parameterized for CI/CD\n- ✅ Probes: Readiness + Liveness\n- ✅ Resources: CPU/Memory limits\n- ✅ Strategy: RollingUpdate for zero-downtime\n\n### 2. Update Kubernetes Service Manifest (8 pts)\n\n**File**: `k8s/service.yaml`\n\n**Required Changes**:\n\n```yaml\napiVersion: v1\nkind: Service\nmetadata:\n  name: medicine-stock-service\n  namespace: medicine-stock\n  labels:\n    app: medicine-stock\n\nspec:\n  type: NodePort  # ← Expose externally\n  \n  selector:\n    app: medicine-stock\n  \n  ports:\n  - port: 5000\n    targetPort: 5000\n    nodePort: 30081  # ← From requirements\n    protocol: TCP\n    name: http\n  \n  # Optional: LoadBalancer for cloud\n  # type: LoadBalancer\n  # annotations:\n  #   cloud.google.com/load-balancer-type: \"Internal\"\n```\n\n**Key Features**:\n- ✅ Type: NodePort (access via http://node-ip:30081)\n- ✅ Port: 5000 (Flask app port)\n- ✅ NodePort: 30081 (external access)\n- ✅ Selector: Routes to medicine-stock pods\n\n### 3. Deployment Script Integration (5 pts)\n\n**File**: `scripts/deploy-k8s.sh` (Create New)\n\n**Purpose**: Automate Kubernetes deployment\n\n```bash\n#!/bin/bash\n\necho \"Deploying Medicine Stock to Kubernetes...\"\n\n# 1. Check prerequisites\nkubectl cluster-info || exit 1\n\n# 2. Create namespace (if not exists)\nkubectl apply -f - <<EOF\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: medicine-stock\nEOF\n\n# 3. Apply manifests\nkubectl apply -f k8s/deployment.yaml\nkubectl apply -f k8s/service.yaml\n\n# 4. Wait for rollout\nkubectl rollout status deployment/medicine-stock \\\n        -n medicine-stock --timeout=5m\n\n# 5. Display status\necho \"\\n✓ Deployment Status:\"\nkubectl get deployment -n medicine-stock\nkubectl get pods -n medicine-stock\nkubectl get svc -n medicine-stock\n\n# 6. Show access information\necho \"\\n✓ Access Information:\"\necho \"Pod Port: http://localhost:5000\"\necho \"NodePort: http://$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[?(@.type==\"InternalIP\")].address}'):30081\"\n```\n\n---\n\n## Testing & Verification\n\n### Test 1: Pod Readiness\n```bash\nkubectl get pods -n medicine-stock -w\n# Wait for \"Running\" status and 1/1 ready\n```\n\n### Test 2: API Connectivity\n```bash\n# Port-forward for testing\nkubectl port-forward svc/medicine-stock-service 5000:5000 -n medicine-stock\n\n# Test endpoint\ncurl http://localhost:5000/\ncurl http://localhost:5000/drugs\n```\n\n### Test 3: NodePort Access\n```bash\n# Get node IP\nNODE_IP=$(kubectl get nodes -o jsonpath='{.items[0].status.addresses[?(@.type==\"ExternalIP\")].address}')\n\n# Access via NodePort\ncurl http://$NODE_IP:30081/\n```\n\n### Test 4: Scaling\n```bash\n# Scale to 3 replicas\nkubectl scale deployment medicine-stock --replicas=3 -n medicine-stock\n\n# Verify\nkubectl get pods -n medicine-stock\n```\n\n### Test 5: Rolling Update\n```bash\n# Update image\nkubectl set image deployment/medicine-stock \\\n        medicine-stock=new-image:new-tag \\\n        -n medicine-stock\n\n# Monitor rollout\nkubectl rollout status deployment/medicine-stock \\\n        -n medicine-stock --timeout=5m\n```\n\n---\n\n## Jenkins Integration\n\n**Update Stage 6 - Kubernetes Deployment**:\n\nThe Stage 6 already includes kubectl deployment. Ensure these parameters:\n\n```groovy\nenvironment {\n    DOCKER_IMAGE = \"your-username/medicine-stock\"  # ← Set correctly\n    KUBE_NAMESPACE = \"medicine-stock\"\n}\n\nparameters {\n    string(name: 'IMAGE_TAG', defaultValue: '${BUILD_NUMBER}')\n}\n\nsteps {\n    sh '''\n        # Set image in deployment\n        kubectl set image deployment/medicine-stock \\\n                medicine-stock=${DOCKER_IMAGE}:${IMAGE_TAG} \\\n                -n medicine-stock\n        \n        # Apply manifests\n        kubectl apply -f k8s/deployment.yaml -n medicine-stock\n        kubectl apply -f k8s/service.yaml -n medicine-stock\n        \n        # Wait for rollout\n        kubectl rollout status deployment/medicine-stock \\\n                -n medicine-stock --timeout=5m\n    '''\n}\n```\n\n---\n\n## Monitoring & Logging\n\n### View Pod Logs\n```bash\n# Real-time logs from one pod\nkubectl logs -f deployment/medicine-stock -n medicine-stock --max-log-requests=2\n\n# Previous pod logs (if crashed)\nkubectl logs -p deployment/medicine-stock -n medicine-stock\n```\n\n### Monitor Metrics\n```bash\n# Pod resource usage\nkubectl top pods -n medicine-stock\n\n# Node resource usage\nkubectl top nodes\n```\n\n### Port Forward to Prometheus\n```bash\n# Access Prometheus from deployment\nkubectl port-forward svc/prometheus-service 9090:9090 -n prometheus\n# Visit: http://localhost:9090\n```\n\n---\n\n## Troubleshooting Common Issues\n\n### Issue 1: Pods stuck in \"Pending\"\n```bash\n# Check events\nkubectl describe pod <pod-name> -n medicine-stock\n\n# Usually: insufficient resources\nkubectl top nodes\nkubectl describe nodes\n```\n\n### Issue 2: Readiness probe failing\n```bash\n# Check pod logs\nkubectl logs <pod-name> -n medicine-stock\n\n# Verify endpoint is responding\nkubectl exec <pod-name> -n medicine-stock -- curl localhost:5000/\n```\n\n### Issue 3: Service not accessible\n```bash\n# Check service\nkubectl get svc -n medicine-stock\nkubectl describe svc medicine-stock-service -n medicine-stock\n\n# Check endpoints\nkubectl get endpoints -n medicine-stock\n```\n\n### Issue 4: Image pull failures\n```bash\n# Check image name\necho $DOCKER_IMAGE:$IMAGE_TAG\n\n# Verify image exists in registry\ndocker pull $DOCKER_IMAGE:$IMAGE_TAG\n\n# Check secret if using private registry\nkubectl get secret -n medicine-stock\n```\n\n---\n\n## Success Criteria\n\n✅ **Phase 4 Complete When:**\n\n1. **Deployment Manifest**\n   - ✓ File: `k8s/deployment.yaml` exists\n   - ✓ Contains 2 replicas\n   - ✓ References ServiceAccount from Phase 3\n   - ✓ Uses ConfigMap and Secret from Phase 3\n   - ✓ Has readiness and liveness probes\n   - ✓ Defines resource limits\n\n2. **Service Manifest**\n   - ✓ File: `k8s/service.yaml` exists\n   - ✓ Type: NodePort\n   - ✓ Port: 5000 (Flask)\n   - ✓ NodePort: 30081\n   - ✓ Selector matches pods\n\n3. **Deployment Execution**\n   - ✓ `kubectl apply -f k8s/deployment.yaml` succeeds\n   - ✓ `kubectl apply -f k8s/service.yaml` succeeds\n   - ✓ Pods reach \"Running\" state\n   - ✓ Readiness probes pass\n   - ✓ Service has endpoints\n\n4. **Verification**\n   - ✓ Run: `kubectl get deployment -n medicine-stock`\n   - ✓ Run: `kubectl get pods -n medicine-stock` (2 running)\n   - ✓ Run: `kubectl get svc -n medicine-stock`\n   - ✓ API responds: `curl localhost:5000/`\n\n5. **Jenkins Integration**\n   - ✓ Stage 6 deploys manifests\n   - ✓ Rollout status monitoring works\n   - ✓ Pod logs accessible\n\n---\n\n## Scoring Breakdown\n\n| Component | Points | Notes |\n|-----------|--------|-------|\n| deployment.yaml | 7 | Replicas, probes, resources, image spec |\n| service.yaml | 5 | NodePort 30081, port 5000 |\n| Manifests applied + verified | 5 | Successful deployment and access |\n| Jenkins integration | 5 | Stage 6 integration working |\n| Documentation | 3 | Quick setup and troubleshooting guide |\n| **Total** | **25 pts** | |\n\n---\n\n## Files to Create/Modify\n\n| File | Action | Status |\n|------|--------|--------|\n| `k8s/deployment.yaml` | Create/Update | ⏳ TODO |\n| `k8s/service.yaml` | Create/Update | ⏳ TODO |\n| `scripts/deploy-k8s.sh` | Create | ⏳ TODO |\n| `scripts/deploy-k8s.bat` | Create | ⏳ TODO |\n| `Jenkinsfile` | Verify Stage 6 | ✅ Done |\n| `PHASE4-KUBERNETES.md` | Create | ⏳ TODO |\n\n---\n\n## Dependencies\n\n- ✅ Phase 3 complete (Namespace, ConfigMap, Secret, RBAC created)\n- ✅ Docker image built and pushed (Phase 2)\n- ✅ Jenkinsfile Stage 6 ready (Phase 2)\n- ⏳ Kubernetes manifests created (Phase 4)\n\n---\n\n## Timeline\n\n**Phase 4 Execution** (After Phase 3 Complete):\n\n1. Create deployment.yaml (15 min)\n2. Create service.yaml (10 min)\n3. Test locally (20 min)\n4. Integration in Jenkins (10 min)\n5. Documentation (15 min)\n6. **Total: ~70 minutes**\n\n---\n\n## Quick Reference\n\n```bash\n# Test local deployment\nkubectl apply -f k8s/\nkubectl rollout status deployment/medicine-stock -n medicine-stock\nkubectl port-forward svc/medicine-stock-service 5000:5000 -n medicine-stock\ncurl http://localhost:5000/drugs\n\n# Scale to 3 pods\nkubectl scale deployment medicine-stock --replicas=3 -n medicine-stock\n\n# View logs\nkubectl logs -f deployment/medicine-stock -n medicine-stock\n\n# Access via NodePort\n# http://node-ip:30081\n```\n\n---\n\n**Next Phase**: Phase 5 - Prometheus + Grafana (Already Complete ✅)\n\n**Final Phase**: Bonus - Presentation\n"
apiVersion: v1
kind: Service
metadata:
  name: medicine-stock-service
  namespace: medicine-stock
spec:
  type: NodePort
  selector:
    app: medicine-stock-api
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080